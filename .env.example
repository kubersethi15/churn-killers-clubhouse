# Supabase (frontend - safe to expose as anon key)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=eyJ...
VITE_SUPABASE_PROJECT_ID=your-project-id

# Note: Service role keys, Anthropic keys, and Resend keys are stored in:
# - GitHub Actions Secrets (for the weekly newsletter workflow)
# - Supabase Edge Function Secrets (for sending emails)
# They should NEVER be committed to this repo or placed in .env
